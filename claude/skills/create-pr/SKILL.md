---
name: create-pr
description: Decide whether to update an existing PR or create a new one, then execute
user-invocable: true
disable-model-invocation: false
---

## Judgment: update existing PR or create new one

Before creating a PR, check:

1. Run `gh pr list --head <current-branch>` to see if a PR already exists for this branch.
2. Run `git log --oneline origin/main..HEAD` to see all unpushed commits on this branch.
3. Decide:
   - **Update existing PR**: If a PR already exists for this branch and the new changes belong to the same logical unit of work, push to the branch and update the PR body if needed.
   - **Create new PR**: If no PR exists, or the changes are a separate concern from the existing PR. In that case, create a new branch from latest `origin/main`, cherry-pick or re-apply changes, then create a new PR.
   - **Ask the user**: If the boundary is ambiguous.

## PR content rules

- Write PR title and body in Japanese.
- Keep content minimal — only include what the reviewer needs to understand and approve the change.
- No greeting, no emoji, no "Generated by" footer, no section headers unless necessary.
- Focus on what changed and why in plain sentences.
- Include concrete before/after comparisons, sample values, or numerical data where applicable.
